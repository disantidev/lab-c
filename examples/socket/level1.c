/*
* Level 1: The Local Echo Server: Build a server that listens on a socket file
* (e.g., /tmp/echo.sock). Use nc -U /tmp/echo.sock to test sending strings to it.
*/

#include <stdio.h>
#include <socket.h>

int main (void) {
  return 0;
}

